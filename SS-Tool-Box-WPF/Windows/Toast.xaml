<Window x:Class="SS_Tool_Box.Windows.Toast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SS_Tool_Box.Windows"
        mc:Ignorable="d"
        Title="Toast"
        Height="580"
        Width="890"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="1,0,0,0" CornerRadius="7" ResizeBorderThickness="0,0,0,0" CaptionHeight="0" NonClientFrameEdges="None"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <StackPanel Margin="0,500,0,0">
            <Button x:Name="Says" MinWidth="70" Foreground="{DynamicResource colorFont}" Style="{DynamicResource Toast}" Background="{DynamicResource colorBg}" HorizontalAlignment="Center" Height="35" VerticalAlignment="Top" Margin="0,0,0,0" Content="这个吐司看起来没好好初始化呢"/>
        </StackPanel>
    </Grid>
</Window>
